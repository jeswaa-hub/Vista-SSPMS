# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sspms?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here  # Required in production; defaults to dev_jwt_secret locally if unset

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend Configuration (Optional)
FRONTEND_URL=https://your-app.railway.app

# Email Configuration (if using email features)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# File Upload Configuration
MAX_FILE_SIZE=10485760 

# Frontend Development
VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_PROXY_TARGET=http://localhost:5000 